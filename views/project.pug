extends layout.pug

block content 
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  article.grid-container.portfolio-intro.portfolio-project
    div.grid-x
      div.cell.small-6.medium-9.large-9
        h1 #{project_name}
        p.lead.text-light #{description||"No description. Some projects are pulled without a description and most don\'t contain a live link except Agora."}
      div.cell.portfolio-meta.small-6.medium-2.large-3
        h6 Technologies
        ul
          each lang in technologies
            li #{lang}
        if (live_link)
          a.button.expanded(href=live_link) Live Demo
        a.button.expanded(href=github_link) GitHub Repo
    
    //- Replace the URLs in the src attributes below with template literals and interpolation to dynamically add three project images.  You only have to add one image.  But adding more images to show your app in different states demonstrates a level of thoroughness and professionalism that potential emplyers will like to see. 
    img.image(src='https://placehold.it/1200x550')
    img.image(src='https://placehold.it/1200x550')
    img.image(src='https://placehold.it/1200x550')